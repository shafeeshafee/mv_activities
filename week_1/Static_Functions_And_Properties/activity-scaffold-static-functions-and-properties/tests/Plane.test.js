const Passenger = require('../src/Passenger')
const Plane = require('../src/Plane')

describe('Plane class', () => {
  const newPlane = new Plane('JFK', 'HEA', '2AX3')
  const newPassenger = new Passenger('Gurum')

  test('Plane instance should have an origin, destination, and flight number', () => {

  })

  test('addPassengers method should add to passenger array', () => {

  })

  test('setOrigin method should set origin property', () => {
   
  })

  test('setDestination method should set destination property', () => {

  })

  test('setFlightNumber method should set flight number', () => {
 
  })
})

