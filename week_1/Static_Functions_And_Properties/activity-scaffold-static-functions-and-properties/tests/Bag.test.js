const Bag = require('../src/Bag');

describe('Bag class', () => {
  const newBag = new Bag(30);

  test('bag should have weight', () => {
  
  })

  test('bag should be of type number', () => {
 
  })
})
