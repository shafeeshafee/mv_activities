const Passenger = require('../src/Passenger')
const Bag = require('../src/Bag')

describe('passenger class', () => {
  const newPassenger = new Passenger('Fatou')
  const newBag = new Bag(30)

  test('passenger instance should have a name of type string and bags property', () => {

  })

  test('bag method should add to bags array', () => {

  })
})
