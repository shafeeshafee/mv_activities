//import database + models
const { db } = require("./db");

//import our models from the file where we've created the associations
const { Music, User } = require("./index");

describe("Favorite music collection database", () => {});
